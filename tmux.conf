# -------------------------------------------------------------------
# General settings --------------------------------------------------
# -------------------------------------------------------------------
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"
set -sg escape-time 0
set -g history-limit 100000
set -g focus-events on
set -g renumber-windows on

# Start index of window/pane with 1, because we're humans, not computers
set -g base-index 1
setw -g pane-base-index 1

setw -g mode-keys vi

# Enable mouse support
set -g mouse on

# Remap prefix from 'C-b' to 'C-a' ----------------------------------
# Press C-a twice to send prefix through ----------------------------
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# reload config by pressing PREFIX r
bind r source-file ~/.tmux.conf \; display-message "Reloaded!"
# Compact window indexes in the current session.
bind R move-window -r \; display-message "Windows renumbered!"

# Open new windows in the current pane directory.
bind c new-window -c "#{pane_current_path}"

# -------------------------------------------------------------------
# Pane management
# -------------------------------------------------------------------
# split window mapping
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# pane movement
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# window switching
bind -r C-h select-window -t :-
bind -r C-l select-window -t :+

# Pane resizing
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# MPD / ncmpcpp
# Ctrl+A, then Option+]. Close the popup with q or Ctrl+C.
bind-key M-] display-popup -E -w 80% -h 80% ncmpcpp

# Keep tmux copy-mode selections available to macOS/Alacritty clipboard.
set -s set-clipboard on

# Enable native Mac OS X copy/paste
#set-option -g default-command "/bin/bash -c 'which reattach-to-user-namespace >/dev/null && exec reattach-to-user-namespace $SHELL -l || exec $SHELL -l'"

# Allow the arrow key to be used immediately after changing windows
set-option -g repeat-time 0

# Do not exit from copy-mode when selecting text.
# @see https://github.com/tmux/tmux/issues/337
# Note: the setting might be renamed MouseDragEndX.
# Depending on whether you activated tmux or vi keybindings (I'm using vi mode):
#bind -temaics-copy MouseDragEnd1Pane copy-selection -x
#bind -tvi-copy MouseDragEnd1Pane copy-selection -x
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-selection

# -------------------------------------------------------------------
# PLUGINS
# -------------------------------------------------------------------
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'uladar/tmux-keyboard-layout'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-open'
#set -g @plugin 'hallazzang/tmux-reset'

set -g @resurrect-capture-pane-contents 'on'

# -------------------------------------------------------------------
# STATUS BAR
# -------------------------------------------------------------------
# set status-bg color67
#set -sg status-left '[#S] '
set -g status-right '#(layout="$(/Users/gv1d/.tmux/plugins/tmux-keyboard-layout/scripts/get_keyboard_layout.sh 2>/dev/null)"; if [ -n "$layout" ]; then printf "%.2s" "$layout" | tr "[:lower:]" "[:upper:]"; else printf "??"; fi) | %a %d/%m %R'
## update status bar every second
set-option -sg status-interval 1

# -------------------------------------------------------------------
# COLOR THEMES 
# -------------------------------------------------------------------
# set -g @plugin "arcticicestudio/nord-tmux"
#disable patched font
# set -g @nord_tmux_no_patched_font "1"
set -g @plugin 'seebi/tmux-colors-solarized'
set -g @colors-solarized '256'

# Note: To view all colors enabled
# for i in {0..255} ; do printf "\x1b[38;5;${i}mcolour${i}\n"; done

# -------------------------------------------------------------------
# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
# run 'Prefix + I' to install plugings
run '~/.tmux/plugins/tpm/tpm'
